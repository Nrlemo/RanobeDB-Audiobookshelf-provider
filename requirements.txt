# RanobeDB Metadata Provider Dependencies
# 
# Core dependencies:
requests>=2.25.0          # HTTP requests library
flask>=2.0.0              # Web framework for HTTP server

# Optional dependencies (comment out to disable):
# gunicorn>=20.0.0        # Production WSGI server
# python-dotenv>=0.19.0   # Environment variable management
# prometheus-client>=0.11.0  # Metrics collection
